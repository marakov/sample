<%= form_for(user.subscribes.find_by(user_id: user.id, channel_id: @channel.id),
             html: {method: :delete},
             remote: true) do |f| %>
  <%= f.submit "Unsubscribe", class: "subscribe-button subbed button", data: {"disable-with": "Unsubscribing..."}%>
<% end %>